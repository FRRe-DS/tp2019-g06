version: "2.1"
services:
  grupo06-db:
    image: mysql:5.7
    restart: always
    environment:
      MYSQL_DATABASE: 'db_turnos'
      # So you don't have to use root, but you can if you like
      MYSQL_USER: 'root'
      # You can use whatever password you like
      MYSQL_PASSWORD: 'Tere2019.'
      # Password for root access
      MYSQL_ROOT_PASSWORD: 'Tere2019.'
    ports:
     - "3306:3306"
